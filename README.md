# LangGraph Checkpoints vs Stores

A GitLab-ready, runnable demo repo that shows the difference between LangGraph **checkpoints** and **stores** with deterministic examples, diagrams, tests, and generated artifacts.

> Status: scaffolding commit. The fun stuff arrives in the next commits.
